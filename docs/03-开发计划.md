# 开发计划与技术选型 v0.1 — IamZhuli

> 第一期 POC 的里程碑划分、技术选型、开发顺序。设计依据见 `docs/01-游戏设计文档.md`,规范见 `docs/02-开发规范.md`。

---

## 0. 总体策略

**核心原则:从"游戏大脑"开始,先让它好玩,再套皮肤。**

1. **纯 C# 类库起步**——撮合引擎、参与者、AI、监管、时间逻辑做成可单元测试的类库,**完全不碰 Unity**。
2. **命令行模拟器验证可玩性**——做一个 `SimCli` 工具,无 UI 跑关卡,打印盘口/成交/AI 决策到终端。能在几秒内验证"散户参数对不对、AI 会不会博弈"。
3. **Unity 做表现层**——大脑验证好玩后,Unity 工程只负责把状态渲染成盘口 UI、处理输入。
4. **每一步都可验证**——每个里程碑结束都有"能跑、能看"的产出,不积累大爆炸式风险。

---

## 1. 技术选型(已定)

| 项 | 选择 | 说明 |
|----|------|------|
| 逻辑层框架 | **.NET 8 类库** | LTS,可单元测试,后期可被 Unity 引用 |
| 单元测试 | **xUnit** | .NET 主流 |
| 表现层引擎 | **Unity 6 LTS**(后期) | 仅做 UI/渲染/输入 |
| 订单簿数据结构 | **`SortedDictionary<decimal,PriceLevel>` + 每价位 `Queue<Order>` + `Dictionary<id,Order>` 撤单索引** | 标准教科书结构,够快好调试 |
| 货币类型 | **`decimal`**(价格建议封装 `Price` 值类型) | 绝不用浮点算钱 |
| 关卡/复盘数据 | **System.Text.Json** | 序列化关卡配置与回放快照 |
| 日志 | **Microsoft.Extensions.Logging** | 标准、可移植 |
| 命令行模拟 | **IamZhuli.SimCli**(控制台程序) | 验证可玩性的关键工具 |
| AI 框架 | **自研轻量状态机 + 意图识别评分** | 不引外部行为树库,逻辑可控可测 |

### 1.1 为什么不引外部撮合/AI 库

- 撮合引擎核心就两三百行,自写比改造别人库更可控、更好调试,且能精确控制游戏所需语义(T+1、涨跌停、对倒识别等)。
- AI 是游戏性核心,自研状态机能精确调参、能在复盘里输出"内心独白"(外部库做不到)。

---

## 2. 里程碑总览

| 里程碑 | 目标 | 产出 | 验证方式 |
|--------|------|------|----------|
| **M0** 环境与骨架 | 能编译能测试 | 解决方案骨架 + 跑通一个 dummy 测试 | `dotnet test` 通过 |
| **M1** 撮合引擎 | 订单簿真实可用 | 完整订单簿 + 限价/市价/撤单 + 涨跌停 | 单元测试全覆盖 |
| **M2** 时间与单参与者 | 能驱动时间、能下单 | tick 循环 + 玩家账户 + 单股盘口能跑 | SimCli 打印逐 tick 盘口 |
| **M3** 散户群体 | 市场有"弹性" | 4 群体聚合行为模型 | SimCli 观察散户推动的盘口形态 |
| **M4** AI 主力 | 有真正的对手 | 状态机 + 意图识别 | SimCli 观察 AI 护盘/洗盘/反杀 |
| **M5** 监管与关卡 | 一关可玩 | 监管关注值 + 关卡配置 + 三星评分 | SimCli 跑完一整关,出评分 |
| **M6** 复盘系统 | 能回看博弈 | 时间轴回放 + AI 日志 + 参与者视角 | SimCli 复盘模式 |
| **M7** Unity 表现层 | 真正能玩的游戏 | Unity 工程:盘口 UI + 下单 + K 线 + 暂停/加速 | 人能坐着玩通一关 |

> M1-M6 是"大脑"阶段(纯类库 + SimCli),M7 是"皮肤"阶段(Unity)。**大脑阶段不依赖 Unity,可独立推进。**

---

## 3. 各里程碑详细分解

### M0 — 环境与骨架(起步)

**目标**:让 `dotnet` 可用,建立可编译的解决方案骨架。

**任务**:
1. 安装 .NET 8 SDK(本机当前未装,见第 4 节环境搭建)。
2. 创建解决方案与初始项目:
   ```
   IamZhuli.sln
   src/IamZhuli.Core/         (共享类型:Price/Quantity/事件/接口)
   src/IamZhuli.Engine/       (撮合引擎)
   tests/IamZhuli.Engine.Tests/
   ```
3. 写一个 dummy 测试验证 xUnit 跑通。
4. 验证 rtk 集成:`rtk dotnet test` 能跑且省 token。

**完成标准**:`rtk dotnet build` 与 `rtk dotnet test` 通过。

---

### M1 — 撮合引擎(核心物理基础)

> 这是整个游戏的物理基础,错了游戏就崩,必须可测。

**任务**:

1. **核心类型**(`IamZhuli.Core`):
   - `Price`(值类型,封装 `decimal`,提供比较/运算)。
   - `Quantity`(`int`,手)。
   - `OrderId`、`ParticipantId`。

2. **订单模型**(`IamZhuli.Engine`):
   - `Order`:类型(限价/市价)、方向(买/卖)、价格、数量、已成交数量、状态(待成交/部分成交/已成交/已撤)、时间戳、所属参与者。
   - `Side` 枚举(Buy/Sell)、`OrderType`(Limit/Market)、`OrderStatus`。

3. **订单簿**(`OrderBook`):
   - 买盘 `SortedDictionary<Price, PriceLevel>`(价格降序,最优买价在前)。
   - 卖盘 `SortedDictionary<Price, PriceLevel>`(价格升序,最优卖价在前)。
   - `PriceLevel` 内部 `Queue<Order>`(时间优先)。
   - `Dictionary<OrderId, Order>` 撤单索引(O(1) 撤单)。

4. **撮合逻辑**(`MatchingEngine`):
   - 新订单进入:限价单挂簿或立即成交(若穿过对方最优价);市价单吃穿对方多档。
   - 成交价 = 被动方价格;现价 = 最近成交价。
   - 边吃边累加 `Σ(价×量)/总量` 得加权均价,产出逐笔 `Trade`(成交记录)。
   - 涨跌停约束:到达涨跌停价后,该方向禁止开仓(只允许平仓)。

5. **T+1 规则**(部分在引擎,部分在账户层):当日买入筹码标记"T 日不可卖”,次日解锁。POC 在账户层实现持仓的 T+1 锁定。

6. **单元测试**(全覆盖,这是 M1 的硬指标):
   - 限价单挂簿排队、价格优先时间优先。
   - 限价单立即成交(穿价)。
   - 市价单吃穿多档、加权均价正确、逐笔 Trade 正确。
   - 撤单(部分成交后撤、挂簿中撤)。
   - 涨跌停阻止开仓。
   - 卖盘空时市价买单剩余处理。

**完成标准**:撮合引擎单元测试 100% 核心路径通过;能精确还原设计文档第 2.5 节"吃货推价"的数值例子。

---

### M2 — 时间系统与玩家账户

**目标**:时间能流动(tick 循环),玩家能下单,一个"只有玩家"的市场能跑起来。

**任务**:

1. **时间系统**(`IamZhuli.Simulation`):
   - `Tick` 为最小推进单位;`SimulationClock` 管理当前 tick、当前交易日(1-30)、日内阶段(盘前/竞价/午休/盘后)。
   - 一个交易日 = 约 300 tick(可配)。30 天 = 一个关卡。
   - 日间一键跳过(进下一交易日盘前)。

2. **玩家账户**:
   - 持仓(含 T+1 锁定:今日买入标记为 `_t1Locked`,次日开盘解锁)。
   - 现金、可用现金、浮盈浮亏。
   - 下单接口:限价买/卖、市价买/卖、撤单。
   - 下单校验:资金够不够、持仓够不够(且未 T+1 锁定)。

3. **tick 循环**(`SimulationLoop`):
   - 每 tick:撮合引擎处理所有待成交订单 → 更新现价 → 产出成交 → 推进时间。
   - 暴露事件:`OnTick`、`OnTrade`、`OnPriceChange`,供表现层/SimCli 订阅。

4. **SimCli M2 版**:命令行驱动,玩家可手动下单,每 tick 打印五档盘口和最新成交。

**完成标准**:SimCli 里手动下单,看到盘口逐档跳动、成交明细、持仓/资金变化、T+1 锁定生效。

---

### M3 — 散户群体(市场弹性)

**目标**:没有 AI 主力,仅散户与玩家,盘口有真实的"弹性"。

**任务**:

1. **4 群体模型**(`IamZhuli.Participants`):
   - 每群体:`数量`、`活跃度`、`平均持仓成本`、触发条件、行为模板。
   - **跟风客**:现价快速上涨 → 追涨买单(聚合)。
   - **抄底盘**:价格急跌到便宜区/破主力成本 → 逢低买单。
   - **止损盘**:价格跌破散户持仓成本 -X% → 恐慌卖单(只能卖历史持仓,T+1)。
   - **价投**:价格偏离内在价值 → 低买高卖。

2. **聚合注入**:每 tick 各群体根据条件生成聚合限价/市价单注入盘口(不模拟个体)。

3. **事件影响**:消息事件修改群体活跃度/情绪参数(POC 先做剧本预设事件)。

4. **历史 K 线**:关卡开始生成一段历史价格序列(随机游走 + 趋势)作为背景。

5. **SimCli M3 版**:观察玩家不操作时,散户推动的盘口自然演化形态。

**完成标准**:不开 AI,玩家静观,盘口呈现有涨有跌、群体行为可辨(如急跌后出现抄底盘承接)的自然形态。

---

### M4 — AI 主力(真正的对手)

**目标**:有一个会博弈的 AI 主力对手。

**任务**:

1. **意图识别器**(`IamZhuli.AI`):
   - 监测盘口信号,为每个意图(吸筹/推价/对倒/挂墙)算置信度。
   - 信号:持续同价位买单、短时大单拉升+放量、对倒特征、反复挂撤大单。

2. **状态机**:
   - 状态:观察/护盘/洗盘/出货/跟风/反杀。
   - 状态切换由意图识别 + 自身持仓成本 + 价格水平驱动。
   - 每状态对应一套行为模板(挂单/吃单/撤单策略)。

3. **AI 账户**:与玩家账户同构,有自己的持仓/成本/资金,持仓对玩家不可见。

4. **AI 个体属性**:策略偏好(激进/保守/狡猾)、反应灵敏度(随难度提升)。

5. **SimCli M4 版**:观察 AI 行为——玩家拉升时 AI 跟风后提前跑,玩家洗盘时 AI 反向护盘吸筹。

**完成标准**:AI 能对玩家操作做出有意义的、非随机的反应;能识别明显操纵并反制。

---

### M5 — 监管与关卡

**目标**:一整关可玩,有目标、有约束、有评分。

**任务**:

1. **监管**(`IamZhuli.Regulator`):
   - 关注值监测:对倒/虚假挂单/异常波动/频繁撤单/大单占比。
   - 惩罚阶梯:30%问询 → 60%罚款 → 80%立案 → 100%强平失败。
   - 关注值衰减。

2. **关卡配置**(数据驱动,JSON):
   - 股票(内在价值、流通盘、历史价)、目标、约束(资金/时间/监管红线/成本)、难度参数(AI/散户/事件频率)、剧本事件时间表。

3. **目标判定**:拉升到位、出货比例、护盘维持等目标的达成判定。

4. **三星评分**:基础达成 + 效率 + 操纵优雅(监管值低位)。

5. **重试**:重置全部状态到关卡初始。

6. **SimCli M5 版**:跑完一整关,输出是否达成、评分、关键节点。

**完成标准**:能跑通至少一个完整关卡(如"拉升出货"),有明确的成败与三星评分。

---

### M6 — 复盘系统

**目标**:关卡结束后能回看整个博弈过程。

**任务**:

1. **回放数据采集**:每个 tick 记录盘口快照 + 所有参与者状态 + AI 决策与内心独白 + 成交明细(含双方身份)。
2. **时间轴回放**:可任意拖动回看任意时刻。
3. **AI 内心独白日志**:文字形式,每决策点记录状态/信号/理由。
4. **参与者视角切换**:玩家/AI/各散户群体的持仓成本资金曲线。
5. **关键事件标注**:自动标记吃货推价/AI 反杀/监管问询等。

**完成标准**:复盘能清晰回放"AI 当时在干什么、为什么",验证 AI 真的在博弈而非随机。

---

### M7 — Unity 表现层

**目标**:人能坐着玩通一关的真正游戏。

**任务**:
1. Unity 6 LTS 工程接入逻辑层类库(作为程序集引用)。
2. 盘口五档 UI + 下单交互(点档位填价、数量条、快捷键)。
3. 分时图 / K 线图渲染。
4. 成交明细滚动、监管条、资金/持仓显示。
5. 暂停(只读)/加速(1x/2x/4x/8x)/日间跳过控制。
6. 关卡选择、目标提示、评分结算、复盘 UI。

**完成标准**:一个非开发者能坐着玩通一关,体验符合设计文档第 1 节的"核心体验一句话"。

---

## 4. 环境搭建(第一步)

本机当前状态:
- ✅ 无 Unity(后期需要,M0-M6 不需要)
- ❌ 无 .NET SDK(M0 必须先装)
- ✅ rtk 已装(`/home/leo/.local/bin/rtk`)

### M0 第一步:安装 .NET 8 SDK

通过 Microsoft 包仓库安装(Ubuntu/Debian),需 sudo。装完验证:`dotnet --version` 显示 8.x。

### M0 第二步:创建解决方案骨架

```
dotnet new sln -n IamZhuli
dotnet new classlib -n IamZhuli.Core -o src/IamZhuli.Core -f net8.0
dotnet new classlib -n IamZhuli.Engine -o src/IamZhuli.Engine -f net8.0
dotnet new xunit -n IamZhuli.Engine.Tests -o tests/IamZhuli.Engine.Tests -f net8.0
# 添加引用关系,加入 sln
```

### M0 第三步:验证 rtk 集成

```
rtk dotnet build
rtk dotnet test
rtk gain
```

---

## 5. 开发顺序与依赖

```
M0 (环境骨架)
  └─ M1 (撮合引擎)  ← 一切的基础,优先级最高
       └─ M2 (时间+玩家账户)
            ├─ M3 (散户)  ─┐
            └─ M4 (AI主力) ─┴─ M5 (监管+关卡) ─ M6 (复盘)
                                                 └─ M7 (Unity 表现层)
```

- M1 是绝对优先,撮合错了后面全错。
- M3/M4 可并行,但都依赖 M2。
- M6 依赖 M4(AI 内心独白需要 AI 决策记录),但可在 M5 后做。
- M7 是最后阶段,但 M2 后即可开始做最简 UI(单参与者盘口),不必等大脑全完成——可并行验证。

---

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| 撮合引擎细节 bug(T+1/涨跌停/穿价) | M1 单元测试全覆盖,作为硬指标 |
| 散户参数难调(盘口太死或太疯) | SimCli 快速迭代调参,几秒看结果 |
| AI 不好玩(太强碾压或太弱无脑) | M4 反复在 SimCli 观察 + 复盘验证 |
| 30 天关卡节奏拖沓 | 加速机制 + 观察期权衡,实测调整 tick 数 |
| Unity 接入时逻辑层不兼容 | 逻辑层用保守 C# 语法;关键逻辑不依赖 .NET 8-only 特性 |

---

## 7. 当前状态与下一步

- **已完成**:设计文档 v0.2、开发规范 v0.1、本开发计划 v0.1、AGENTS.md。
- **下一步**:执行 M0(安装 .NET SDK + 建骨架),然后进入 M1(撮合引擎)。
- **待用户确认**:是否现在开始执行 M0?
