# 架构演进开发方案 v0.1 — IamZhuli

> 基于 `docs/05-市场初始化与预演设计.md` 的实施方案。
> 回答:现有代码怎么改、新建什么、按什么顺序、每步怎么验证。
> 依据:对现有代码的完整审查(撮合/账户/loop/参与者/AI/采集器/监管/关卡)。

---

## 0. 架构现状(审查结论)

### 0.1 好消息:大部分可复用

| 模块 | 判定 | 说明 |
|------|------|------|
| MatchingEngine/OrderBook/Trade | ✅ 零改 | 撮合核心稳定 |
| TradingSession | ✅ 零改 | 事件/账户/撮合路由完备 |
| SimulationClock | ✅ 零改 | |
| Account/Position | ✅ 零改 | 权益曲线由外部采集器持有 |
| Regulator | ✅ 零改 | 玩家不参与时天然静默 |
| SimulationLoop | ✅ 零改 | **预演直接复用它跑历史(临时实例)** |
| MarketDataCollector | ✅ 零改 | 预演照常采集历史K线 |
| RetailProfilePool | ✅ 零改 | 预演中正常 Act,情绪/持仓涌现 |
| AIMainForce 全套 | ✅ 复用(蓝本) | InstitutionB 参考其结构 |

### 0.2 需改造/新建

| 模块 | 判定 | 关键改动 |
|------|------|---------|
| MarketHistory | 🟡 改造/降级 | 随机游走降级为 MarketScenario 的一种实现 |
| LevelDefinition | 🟡 改造 | 加 ScenarioType;Objectives 在积分制下变可选 |
| LevelJudge | 🟡 改造 | 目标制三星替换为 ScoreCalculator |
| GameSingleton.LoadLevel | 🟡 改造 | 拆 BuildMarket/WireRuntimeCallbacks,中间插预演 |
| MarketMaker | 🔴 替换 | 由 InstitutionB 取代 |
| MarketScenario | 🆕 新建 | 剧本(趋势生成+情绪标签) |
| GuidanceMarketMaker | 🆕 新建 | 预演专用,锚定K线价,预演后弃用 |
| InstitutionB | 🆕 新建 | 控盘型AI+基础做市 |
| MarketPreplay | 🆕 新建 | 预演执行器(临时loop+迁移状态) |
| ScoreCalculator | 🆕 新建 | 收益率+风险调整 |
| EquityCurveCollector | 🆕 新建 | 三方权益序列(回撤/波动率/排名) |

### 0.3 关键洞察(影响方案)

1. **预演复用 SimulationLoop,不新建 PreplayLoop**:用临时实例跑历史,跑完迁移账户状态,丢弃临时 loop(含 GuidanceMM 的无限资金账户)。天然隔离事件订阅。
2. **Account 无需改**:权益曲线由外部 EquityCurveCollector 订阅事件采集(仿 MarketDataCollector 模式)。
3. **IParticipant 是统一接口**:GuidanceMM/InstitutionB/剧本参与者都实现它,这是预演能复用 loop 的根本。
4. **MarketMaker 替换影响面小**:直接引用仅 GameSingleton 1 处;下游读盘口深度的代码(IntentRecognizer/散户进场/AI)行为会因盘口变浅而变化,需重新调参但无需改代码。

---

## 1. 四阶段开发计划(顺序+依赖)

```
阶段1: 机构B(第二AI主力) ──┐
                           ├─→ 阶段3: 预演引擎(依赖1+2)
阶段2: 剧本系统 ───────────┘          
                           ──→ 阶段4: 积分系统(可独立,但结算依赖预演产出的三方对比)
```

- 阶段1、2 可并行(无依赖),但建议先1后2(机构B更影响手感)
- 阶段3 依赖 1+2(预演里要有机构B和剧本)
- 阶段4 可独立开发,但完整体验依赖预演产出的真实初始状态

---

## 2. 阶段1:机构B(做市+风险控制+操盘三合一) ✅ 完成

### 2.1 目标
用"合二为一"的机构B 取代无限做市商。机构B 同时承担:
- **做市职能**:正常时双边挂单提供流动性
- **风险控制**:动态评估风险(持仓偏离/方向/波动/冲击),风险高时收紧流动性
- **操盘职能**:风险高时转为方向操作(减仓/反手)

**关键决策(合二为一)**:做市商和机构B 是同一主体。资金理论上无限,但通过风险控制形成"软约束"——盘口会变薄但不会突然断层。

### 2.2 设计:风险控制驱动流动性

```
机构B 每 tick 评估综合风险值(0~1):
  持仓偏离: 净持仓占总资产比例(接太多货→紧张)
  方向风险: 现价 vs 公允价值(偏离大→怕被套)
  波动风险: 近期波动率(波动大→扩大价差)
  冲击频率: 近期被单边吃的速度(怀疑有主力→收紧)

风险低(<0.3): 正常双边挂单,深度充足
风险中(0.3~0.7): 扩大价差、减少挂单量
风险高(>0.7): 大幅减少挂单,管理敞口
风险极高(>0.9): 停止做市,转为方向操作(可能反手)
```

流动性的"软约束":玩家体验是越推越费劲,而非推到一半撞墙。

### 2.3 新建文件

**`src/IamZhuli.Simulation/AI/InstitutionB.cs`**
- IParticipant 实现,合三为一
- 组成:
  - `MarketMakerRiskController`:风险评估(持仓偏离/方向/波动/冲击)
  - 做市逻辑:根据风险等级调整挂单(深度/价差)
  - 操盘逻辑:风险极高时转为方向操作(参考 AIMainForce 的行为模板)
  - `Thoughts` 日志(供复盘)
- 复用 `IntentRecognizer`(读盘口意图)+ 内置风险评估

### 2.4 改造文件

**`tools/IamZhuli.Web/GameSingleton.cs`**
- `MarketMaker` 替换为 `InstitutionB`
- `MarketMaker` ParticipantId 改为机构B的 id

### 2.5 保留
- `MarketMaker.cs` 保留(工具类),但不再被 GameSingleton 引用

### 2.6 验证标准
- 玩家持续吃货 → 卖盘逐步变薄(风险升高),而非无限
- 机构B 在风险高时主动减少挂单/管理敞口
- 机构B 的 Thoughts 有风险/做市/操盘相关决策
- 现有 61 个测试不回归

### 2.7 风险
- 风险控制参数需调参(持仓偏离容忍度/方向阈值等)
- 盘口变浅影响下游(IntentRecognizer/散户进场/AI)→ 需重新调参但无需改代码

---

## 3. 阶段2:剧本系统 ⚠️ 部分完成

> **状态**:MarketScenario 已实现(Decline/Rally/Sideways/VReversal),但 **LevelDefinition 未加 ScenarioType 字段**——当前 GameSingleton 硬编码 `ScenarioType.Decline`。场景类型未与关卡挂钩,待后续接入。

### 2.1 目标
根据剧本类型生成30天历史K线(下跌/上涨/横盘/V型),作为预演的价格引导目标。

### 2.2 新建文件

**`src/IamZhuli.Simulation/Scenarios/MarketScenario.cs`**
```csharp
public enum ScenarioType { Decline, Rally, Sideways, VReversal }

public sealed class MarketScenario
{
    ScenarioType Type { get; }
    Price StartPrice { get; }      // 历史起点(如12元)
    Price EndPrice { get; }        // 历史终点=玩家进场价(如10元)
    int Days { get; }              // 30
    decimal Volatility { get; }    // 日波动率
    /// <summary>生成每天的目标价序列(预演引导用)。</summary>
    List<decimal> GenerateDailyTargets(int? seed);
}
```
- 下跌剧本:StartPrice(12) 线性/随机衰减到 EndPrice(10),带波动
- 上涨剧本:StartPrice(8) 涨到 EndPrice(10)
- 横盘:围绕 EndPrice 震荡
- V型:先跌后涨

### 2.3 改造文件

**`src/IamZhuli.Simulation/MarketHistory.cs`**
- `Generate` 降级为 `RandomScenario` 的实现(或保留作为工具方法)
- MarketScenario 内部可调用 MarketHistory 的 Candle 结构

**`src/IamZhuli.Simulation/Levels/LevelDefinition.cs`**
- 新增 `ScenarioType Scenario { get; init; }` 字段
- 工厂方法(Tutorial/PumpAndDump/Accumulate)配对应剧本

### 2.4 验证标准
- 生成的K线符合剧本特征(下跌剧本真的下跌、上涨真的上涨)
- 同一剧本+不同seed 有随机扰动(不可背诵)

---

## 4. 阶段3:预演引擎(核心) ✅ 完成

### 4.1 目标
玩家进场前,散户+AI+机构B 跑完历史K线,持仓/成本/情绪/盘口全部真实涌现。

### 4.2 新建文件

**`src/IamZhuli.Simulation/Preplay/GuidanceMarketMaker.cs`**
- IParticipant 实现
- 构造接收 `MarketScenario`(每天目标价)
- Act:按当天目标价在 ±0.02 范围挂大单(无限资金,锚定价格)
- 预演结束后随临时 loop 弃用(不迁移其账户)

**`src/IamZhuli.Simulation/Preplay/MarketPreplay.cs`**
```csharp
public sealed class MarketPreplay
{
    /// <summary>跑预演,产出初始状态。</summary>
    public PreplayResult Run(MarketScenario scenario, LevelDefinition level)
    {
        // 1. 创建临时 loop(同一 MarketRules/Stock)
        // 2. 添加参与者:散户池+AI+机构B+GuidanceMM+MarketDataCollector
        //    (玩家账户不 AddParticipant,但可预创建)
        // 3. 按 scenario 的日目标价,逐天跑 ticksPerDay 个 tick
        //    · 每天 GuidanceMM 锚定当天目标价
        //    · 散户/AI/机构B 在被引导的价格环境里真实决策
        // 4. 跑完,迁移状态到游戏 loop:
        //    · 各参与者账户的 Cash/Position(直接复用同一 Session?或迁移)
        //    · MarketDataCollector 的 DailyCandles/PreviousClose
        //    · 情绪指数(MarketSentiment)
        // 5. 弃用临时 loop(含 GuidanceMM 账户)
    }
}
public sealed class PreplayResult
{
    List<DailyCandle> HistoryCandles;
    decimal PreviousClose;
    decimal InitialSentiment;
    // 参与者状态通过共享 Session 直接迁移(见4.3)
}
```

### 4.3 关键设计:状态迁移方式

**方案A(推荐):预演和游戏共用同一个 TradingSession/MatchingEngine**
- 预演用临时 SimulationLoop,但 Session/Engine 是游戏 loop 的同一个实例
- 预演跑完,参与者账户状态天然就在 Session 里(无需迁移)
- 只需迁移:MarketDataCollector 的数据(用预演的 collector)、情绪指数
- 丢弃临时 loop(含 GuidanceMM,它的账户留在 Session 但不再 Act)

**方案B:独立 Session,跑完迁移账户**
- 更隔离但迁移复杂(要深拷贝账户状态)
- 不推荐

### 4.4 改造文件

**`tools/IamZhuli.Web/GameSingleton.cs` — LoadLevel 拆分**
```csharp
void LoadLevel(LevelDefinition level)
{
    // ... 重置状态
    BuildMarket(level);           // 构建 Engine/Loop/Session/参与者(不含 GuidanceMM)
    MarketPreplay.Run(loop, scenario, level);  // 预演(临时驱动,玩家不Act)
    WireRuntimeCallbacks();       // 接线监管/权益采集/失败判定(预演后才接)
    loop.Start();                 // 玩家可操作
}
```

### 4.5 验证标准(最关键)
- 预演后散户持仓/成本真实(下跌剧本→散户被套成本高)
- 情绪符合剧本(下跌→冰点<30、上涨→过热>70)
- 盘口有真实挂单(各参与者意愿单)
- 历史K线显示在图表背景
- 玩家进场即面对真实市场状态

### 4.6 风险
- 引导做市商强度调参(太强→参与者行为无关,太弱→价格偏离K线)
- 预演时长(1800tick)的性能(毫秒级,应无问题)
- 共享 Session 方案下 GuidanceMM 账户残留(不影响,它不 Act)

---

## 5. 阶段4:积分系统 ✅ 完成(并行运行,非替换)

> **状态说明**:ScoreCalculator + EquityCurveCollector 已实现并接入(`/api/score`)。但原计划说"替换 LevelJudge 的三星评分",实际是**两者并行**:EndLevel 仍输出三星+目标达成,SettleScore 额外输出三方收益率排名。前端结算页同时显示两者。

### 5.1 目标
风险调整后收益率作为唯一胜负标准,三方(玩家/AI/机构B)对比排名。

### 5.2 新建文件

**`src/IamZhuli.Simulation/MarketData/EquityCurveCollector.cs`**
- 仿 MarketDataCollector,订阅 loop.OnTick/OnNewDay
- 每 tick/每日用 Engine.LastPrice 对玩家、AI、机构B 算 TotalEquity
- 维护三方权益序列 `List<decimal>`
- 暴露:最大回撤、波动率、当前收益率

**`src/IamZhuli.Simulation/Levels/ScoreCalculator.cs`**
```csharp
public sealed record ScoreResult(
    decimal ReturnRate,           // 收益率
    decimal MaxDrawdown,          // 最大回撤
    decimal RiskAdjustedScore,    // 最终得分
    int Rank,                     // 三方排名(1=第一)
    string Comment);              // AI教练评价

public sealed class ScoreCalculator
{
    ScoreResult Calculate(Account player, Account aiA, Account institutionB,
                          EquityCurveCollector curves, Regulator regulator,
                          decimal initialCash, Price? lastPrice);
}
```
- 收益率 = (最终权益 - 初始) / 初始
- 风险调整 = 收益率 × (1 - 最大回撤×权重 - 波动率×权重 - 监管峰值×权重)
- 三方对比:对 AI/机构B 也算同样得分,排名

### 5.3 改造文件

**`src/IamZhuli.Simulation/Levels/LevelJudge.cs`**
- `Settle` 的目标制三星(第103行)替换为调 `ScoreCalculator`
- `LevelResult` 扩展 Score/Rank 字段
- `EvaluateProgress` 改为返回收益率/回撤等实时指标(替代目标进度)

**`tools/IamZhuli.Web/GameSingleton.cs`**
- 持有 EquityCurveCollector(订阅游戏 loop)
- EndLevel 调 ScoreCalculator
- 快照加实时收益率/回撤/排名

**`tools/IamZhuli.Web/Dtos.cs` + 前端**
- 结算页改为收益率+排名展示
- 实时面板加收益率/回撤

### 5.4 验证标准
- 结算显示三方收益率排名
- 最大回撤大的得分低(风险调整生效)
- 不同剧本下得分差异合理

---

## 6. 跨阶段的调参点(实施后必做)

机构B替换+预演接入后,盘口深度变化会导致下游行为变化,需要重新调参:
- `IntentRecognizer`:深度失衡阈值(盘口变浅,失衡更易触发)
- `RetailProfilePool`:进场参考深度的逻辑
- `AIMainForce`:Defend/Follow 的深度判断
- `Regulator`:大单/波动阈值(盘口浅→大单冲击更明显)
- `InstitutionB`:做市补单量/资金规模(核心调参)

---

## 7. 文件改动总览

### 7.1 新建(8个)
```
src/IamZhuli.Simulation/
├── AI/InstitutionB.cs                  (阶段1)
├── Scenarios/MarketScenario.cs         (阶段2)
├── Preplay/GuidanceMarketMaker.cs      (阶段3)
├── Preplay/MarketPreplay.cs            (阶段3)
├── MarketData/EquityCurveCollector.cs  (阶段4)
└── Levels/ScoreCalculator.cs           (阶段4)
tests/                                  (每阶段对应测试)
```

### 7.2 改造(5个)
```
tools/IamZhuli.Web/GameSingleton.cs     (阶段1,3,4 — LoadLevel拆分+预演+积分)
src/IamZhuli.Simulation/Levels/LevelDefinition.cs  (阶段2,4 — 加Scenario/改目标)
src/IamZhuli.Simulation/Levels/LevelJudge.cs       (阶段4 — 积分制)
src/IamZhuli.Simulation/MarketHistory.cs           (阶段2 — 降级)
src/IamZhuli.Simulation/AI/AIStateMachine.cs       (阶段1 — StrategyProfile加Controlling)
```

### 7.3 不变(核心稳定层)
撮合引擎、TradingSession、Account、SimulationLoop、Clock、Regulator、MarketDataCollector、RetailProfilePool、AIMainForce、所有 DTO/前端图表

---

## 8. 建议的开发顺序与里程碑

| 里程碑 | 内容 | 可验证产出 |
|--------|------|-----------|
| **M-A** | 阶段1:机构B | 盘口会枯竭、机构B主动操盘 |
| **M-B** | 阶段2:剧本系统 | 生成符合特征的K线 |
| **M-C** | 阶段3:预演引擎 | 玩家进场即面对真实状态(持仓/情绪/盘口涌现) |
| **M-D** | 阶段4:积分系统 | 三方收益率排名结算 |

每个里程碑结束:测试通过 + Web 实测 + 提交。

---

## 9. 待确认的工程细节

1. **预演的 Session 共享**:方案A(共享Session) vs 方案B(独立迁移)。倾向A,但需确认 GuidanceMM 账户残留无害。
2. **InstitutionB 是否复用 AIStateMachine**:走"扩展StrategyProfile"还是"独立控盘状态机"。倾向后者(控盘逻辑差异大)。
3. **EquityCurveCollector 采样频率**:每tick(细)还是每日(粗,够算回撤)。倾向每日+关键tick。
4. **风险调整权重**:最大回撤/波动率/监管值各占多少。需实测平衡。
5. **预演性能**:1800tick 的同步预演是否影响加载体验(预期毫秒级,需实测)。
